{% include 'default/_form.html.twig' with {
    page_title: 'title.shorten_delete'|trans({'%id%': shorten.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'shorten_index',
} %}
